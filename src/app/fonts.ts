import { Inter, Advent_Pro, Antonio } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

export const adventPro = Advent_Pro({
    weight: ['400', '700'],
    style: ['normal', 'italic'],
    subsets: ['latin'],
    display: 'swap',
});

export const antonio = Antonio({
    subsets: ['latin'],
    display: 'swap',
});